---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<BaseLayout title="Inicio | Nelson Martínez - Ingeniero en Sistemas">
  <section class="hero">
    <div class="hero-content">
      <div class="hero-text">
        <h1 class="title">Nelson Martínez <span class="highlight">👨🏿‍💻</span></h1>
        <h2 class="subtitle">Ingeniero en Sistemas</h2>
        <p class="tagline">Con conocimeintos en <span class="typed-text"></span></p>
        <div class="cta-buttons">
          <a href="/projects" class="cta-button primary">
            Ver Proyectos
          </a>
          <a href="/contact" class="cta-button secondary">
            Contáctame
          </a>
        </div>
      </div>
      <div class="hero-image">
        <div class="tech-icons">
          <div class="icon-circle">🌐</div>
          <div class="icon-circle">🔌</div>
          <div class="icon-circle">💻</div>
          <div class="icon-circle">🧠</div>
        </div>
      </div>
    </div>
  </section>

  <section class="features">
    <div class="feature-card">
      <div class="feature-icon">🔌</div>
      <h3>Redes Cisco</h3>
      <p>Configuración avanzada de routers y switches con certificación CCNA</p>
    </div>
    <div class="feature-card">
      <div class="feature-icon">💻</div>
      <h3>Desarrollo Web</h3>
      <p>Full-stack development con tecnologías modernas</p>
    </div>
    <div class="feature-card">
      <div class="feature-icon">🤖</div>
      <h3>Machine Learning</h3>
      <p>Modelos predictivos con Python y TensorFlow</p>
    </div>
  </section>

  <section class="recent-projects">
    <h2>Proyectos Destacados</h2>
    <div class="projects-preview">
      <div class="project-card">
        <h3>Generador de Reportes</h3>
        <p>Proyecto académico</p>
        <a href="/expertos" class="project-link">Ver más →</a>
      </div>
      <div class="project-card">
        <h3>Plataforma del Artículo 140 UNAH</h3>
        <p>Sistema de identificación con Python</p>
        <a href="https://articulo140-voae.unah.edu.hn/landing" class="project-link">Ver más →</a>
      </div>
    </div>
    <a href="/projects" class="view-all">Ver todos los proyectos</a>
  </section>
</BaseLayout>

<style>
  /* Hero Section */
  .hero {
    min-height: 80vh;
    display: flex;
    align-items: center;
    background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
    padding: 0 2rem;
  }

  .hero-content {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    align-items: center;
  }

  .title {
    font-size: 3.5rem;
    margin-bottom: 0.5rem;
    color: #1a1a2e;
    line-height: 1.2;
  }

  .highlight {
    color: #2A5C9A;
  }

  .subtitle {
    font-size: 1.8rem;
    color: #2A5C9A;
    margin-bottom: 1.5rem;
    font-weight: 600;
  }

  .tagline {
    font-size: 1.5rem;
    color: #4a5568;
    margin-bottom: 2rem;
  }

  .typed-text {
    color: #2A5C9A;
    font-weight: 600;
    border-right: 2px solid #2A5C9A;
    animation: blink 0.75s step-end infinite;
    display: inline-block;
    min-width: 120px;
  }

  .cta-buttons {
    display: flex;
    gap: 1rem;
    margin-top: 2rem;
  }

  .cta-button {
    padding: 1rem 2rem;
    border-radius: 8px;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s ease;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
  }

  .primary {
    background: #2A5C9A;
    color: white;
    border: 2px solid #2A5C9A;
  }

  .primary:hover {
    background: #1E4A8A;
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(42, 92, 154, 0.2);
  }

  .secondary {
    background: transparent;
    color: #2A5C9A;
    border: 2px solid #2A5C9A;
  }

  .secondary:hover {
    background: rgba(42, 92, 154, 0.1);
    transform: translateY(-2px);
  }

  .tech-icons {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
  }

  .icon-circle {
    width: 100px;
    height: 100px;
    background: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2.5rem;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
    transition: all 0.3s ease;
  }

  .icon-circle:hover {
    transform: translateY(-5px) scale(1.1);
  }

  /* Features Section */
  .features {
    max-width: 1200px;
    margin: 5rem auto;
    padding: 0 2rem;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }

  .feature-card {
    background: white;
    padding: 2rem;
    border-radius: 12px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
    transition: transform 0.3s ease;
    text-align: center;
  }

  .feature-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
  }

  .feature-icon {
    font-size: 2.5rem;
    margin-bottom: 1rem;
    color: #2A5C9A;
  }

  .feature-card h3 {
    font-size: 1.5rem;
    margin-bottom: 1rem;
    color: #1a1a2e;
  }

  /* Projects Section */
  .recent-projects {
    max-width: 1200px;
    margin: 5rem auto;
    padding: 0 2rem;
    text-align: center;
  }

  .recent-projects h2 {
    font-size: 2.5rem;
    margin-bottom: 3rem;
    color: #1a1a2e;
  }

  .projects-preview {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    margin-bottom: 2rem;
  }

  .project-card {
    background: white;
    padding: 2rem;
    border-radius: 12px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
    transition: all 0.3s ease;
    text-align: left;
    border-left: 4px solid #2A5C9A;
  }

  .project-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
  }

  .project-card h3 {
    font-size: 1.5rem;
    margin-bottom: 0.5rem;
    color: #1a1a2e;
  }

  .project-link {
    color: #2A5C9A;
    font-weight: 600;
    text-decoration: none;
    display: inline-block;
    margin-top: 1rem;
    transition: all 0.3s ease;
  }

  .project-link:hover {
    color: #1E4A8A;
    transform: translateX(5px);
  }

  .view-all {
    display: inline-block;
    margin-top: 2rem;
    padding: 0.8rem 2rem;
    background: #2A5C9A;
    color: white;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 600;
    transition: all 0.3s ease;
  }

  .view-all:hover {
    background: #1E4A8A;
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(42, 92, 154, 0.2);
  }

  /* Animations */
  @keyframes blink {
    from, to { border-color: transparent }
    50% { border-color: #2A5C9A }
  }

  /* Responsive */
  @media (max-width: 768px) {
    .hero-content {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .title {
      font-size: 2.5rem;
    }

    .subtitle {
      font-size: 1.5rem;
    }

    .cta-buttons {
      justify-content: center;
    }

    .tech-icons {
      margin-top: 3rem;
      justify-content: center;
    }
  }
</style>

<script>
  // Efecto de texto que escribe
  const typedTextSpan = document.querySelector('.typed-text');
  const textArray = ['Redes Cisco', 'Desarrollo Web', 'Machine Learning'];
  const typingDelay = 100;
  const erasingDelay = 50;
  const newTextDelay = 1500;
  let textArrayIndex = 0;
  let charIndex = 0;

  function type() {
    if (charIndex < textArray[textArrayIndex].length) {
      typedTextSpan.textContent += textArray[textArrayIndex].charAt(charIndex);
      charIndex++;
      setTimeout(type, typingDelay);
    } else {
      setTimeout(erase, newTextDelay);
    }
  }

  function erase() {
    if (charIndex > 0) {
      typedTextSpan.textContent = textArray[textArrayIndex].substring(0, charIndex-1);
      charIndex--;
      setTimeout(erase, erasingDelay);
    } else {
      textArrayIndex++;
      if(textArrayIndex>=textArray.length) textArrayIndex=0;
      setTimeout(type, typingDelay + 500);
    }
  }

  document.addEventListener('DOMContentLoaded', function() {
    setTimeout(type, 1000);
  });
</script>